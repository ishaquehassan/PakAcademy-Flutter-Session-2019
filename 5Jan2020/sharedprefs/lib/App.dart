import 'package:flutter/material.dart';
import 'package:sharedprefs/pages/Home.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Home(),
    );
  }
}
